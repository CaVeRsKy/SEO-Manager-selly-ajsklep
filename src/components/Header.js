import React from 'react';
import './Header.css'; // Zakładając, że masz plik Header.css do stylizacji nagłówka

const Header = () => {
    return (
        <header>
            <h1>SEO Manager for Selly</h1>
        </header>
    );
};

export default Header;
